{
  "name": "sulmina-barbers",
  "version": "0.1.0",
  "private": true,
  "description": "Sulmina Barber Shop — booking website",
  "type": "commonjs",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env --watch server.js",
    "preview": "set PREVIEW_NO_DB=true&& node --watch --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "ejs": "^5.0.2",
    "express": "^5.2.1",
    "mongodb": "^7.1.1",
    "nodemailer": "^8.0.5"
  }
}
